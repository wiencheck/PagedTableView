    import XCTest
    @testable import PagedTableView

    final class PagedTableViewTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(PagedTableView().text, "Hello, World!")
        }
    }
